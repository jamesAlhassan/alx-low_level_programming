#include <stdio.h>
/**
 * main - Prints the alphabets in lower case
 * Return: 0 if success
 */
int main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++);

{
putchar(alphabet);
}
return (0);
}
